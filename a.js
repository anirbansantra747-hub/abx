// Fix missing test by adding a test file (e.g., add.test.js)
// Use Jest or Mocha for testing
// Adhere to testing framework's recommended structure

function add(a, b) {
  // Correct indentation to 2 spaces
  // Consistent indentation improves readability
  return a + b;
}